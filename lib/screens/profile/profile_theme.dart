import 'package:flutter/material.dart';

/// Theme bleu et blanc de la page Profil (maquette : bouton "modifier" bleu,
/// bouton "ajouter publication" en contour, cartes grises neutres).
class ProfileColors {
  static const background = Colors.white;
  static const card = Color(0xFFF6F6F4);
  static const border = Color(0xFFE9E5DF);
  static const accent = Color(0xFF0A66C2);
  static const textPrimary = Color(0xFF181818);
  static const textSecondary = Color(0xFF585C60);
  static const textFaint = Color(0xFF9C9C9C);
  static const red = Color(0xFFFF5C5C);
}
